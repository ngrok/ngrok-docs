---
title: Phase Rules
description: Learn about phase rules in Traffic Policy that combine expressions and actions to filter and process traffic within each phase.
---

Each Phase is made up of Phase Rules.
Phase Rules allow you to define how traffic is filtered and processed within a phase.
To do this each rule consists of two key parts: expressions and actions.

Here is an example:

```yaml
on_tcp_connect:
  - expressions:
      - conn.client_ip == '192.168.1.200'
    actions:
      - type: deny
```

## Overall structure

A Traffic Policy document is a YAML (or JSON) object whose top-level keys are [phase](/traffic-policy/concepts/phases) names.
Each phase contains a list of rules.
Every rule may optionally carry a human-readable name and one or more CEL expressions, plus the list of actions that run when all expressions match.
Include only the phases you need; the same structure applies to `on_http_request` and `on_http_response`.

```yaml
# Phase names are the top-level keys.
on_tcp_connect:           # runs when a TCP connection is established
  - name: "rule name"     # optional — a human-readable label for the rule
    expressions:          # optional — all conditions must be true for the rule to run
      - "expression"      # CEL expression (e.g. conn.client_ip == '1.2.3.4')
    actions:              # required — one or more actions to execute in order
      - type: "action-type"
        config:           # optional — action-specific configuration
          key: value
```

A policy only needs to include the phases it uses.
Each phase holds an ordered list of rules, and each rule requires at least one action.

### Rule fields

| Field | Required | Description |
|-------|----------|-------------|
| `name` | No | A human-readable label for the rule. |
| `expressions` | No | A list of [CEL expressions](/traffic-policy/concepts/expressions). All must evaluate to `true` for the actions to run. Omitting `expressions` matches all traffic. |
| `actions` | Yes | An ordered list of [actions](/traffic-policy/concepts/actions) to execute. Each action requires a `type` and an optional `config` block. |

### Annotated example with multiple rules and phases

The following example shows a complete policy that uses two phases and multiple rules:

<CodeGroup>
```yaml policy.yml
on_http_request:
  # Rule 1: block a specific IP address
  - name: "block bad actor"
    expressions:
      - "conn.client_ip == '1.2.3.4'"
    actions:
      - type: deny
        config:
          status_code: 403

  # Rule 2: require authentication on /admin paths
  - name: "admin oauth"
    expressions:
      - "req.url.path.startsWith('/admin')"
    actions:
      - type: oauth
        config:
          provider: google

  # Rule 3: no expressions — forward all remaining traffic to the upstream
  - actions:
      - type: forward-internal
        config:
          url: https://example.internal

on_http_response:
  # Rule 1: add a security header to every response
  - actions:
      - type: add-headers
        config:
          headers:
            x-frame-options: "DENY"
```

```json policy.json
{
  "on_http_request": [
    {
      "name": "block bad actor",
      "expressions": ["conn.client_ip == '1.2.3.4'"],
      "actions": [
        { "type": "deny", "config": { "status_code": 403 } }
      ]
    },
    {
      "name": "admin oauth",
      "expressions": ["req.url.path.startsWith('/admin')"],
      "actions": [
        { "type": "oauth", "config": { "provider": "google" } }
      ]
    },
    {
      "actions": [
        { "type": "forward-internal", "config": { "url": "https://example.internal" } }
      ]
    }
  ],
  "on_http_response": [
    {
      "actions": [
        { "type": "add-headers", "config": { "headers": { "x-frame-options": "DENY" } } }
      ]
    }
  ]
}
```
</CodeGroup>

## Expressions

Expressions are conditions written in [Common Expression Language (CEL)](https://github.com/google/cel-spec) that can be used to evaluate specific traffic attributes, such as the client IP, request URL, or HTTP method.
These conditions determine whether a rule applies to a given traffic flow.
For instance, an expression like `conn.client_ip == '192.168.1.200'` targets requests from a specific IP address.

You can define multiple expressions, which are automatically combined using the `&&` Operator.
This means all expressions in the list must evaluate to `true` for the associated rules to run.
If no expressions are provided, the system defaults to `true`, ensuring the rule matches all traffic, and the specified actions are executed in sequence.

By combining multiple conditions, you can craft highly specific and flexible rules to manage traffic effectively.

## Actions

Actions define the behavior that is applied when the expressions evaluate to true.
Each action specifies a particular operation to be applied, such as denying traffic, modifying headers, or redirecting requests.
Actions are executed sequentially as defined in the policy, but note that some actions can short-circuit the request and return without executing subsequent actions.

For example, the following action denies traffic and returns a `404` HTTP status code:

```yaml
on_http_request:
  - actions:
      - type: "deny"
        config:
          status_code: 404
```

For a full list of actions, check out the [Action Hub](../actions).

## Chaining rules and priority

Multiple rules can be defined within a single phase.
Rules are evaluated in the order they are defined, and their execution depends on the type of actions taken.
For example:

- Some actions (for example, `deny`) immediately stop further rule evaluation.
- Others (for example, `url-rewrite`) allow subsequent rules to apply.

This flexibility enables layered and powerful Traffic Policies.
