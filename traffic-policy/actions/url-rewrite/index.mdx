## Overview

The **URL Rewrite** Traffic Policy action enables you to modify the incoming
request URL using regular expressions before it reaches the upstream server,
without changing the URL seen by the client.

This action is useful for routing users without exposing internal system
details.
