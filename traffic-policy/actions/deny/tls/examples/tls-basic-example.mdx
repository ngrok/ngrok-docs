import ConfigExample from "/src/components/ConfigExample.tsx";

### Deny all traffic

The following [Traffic Policy](/docs/tls/traffic-policy/)
configuration will deny all inbound traffic on your endpoint.

#### Example Traffic Policy Document

<ConfigExample
	snippetText={null}
	showLineNumbers={true}
	yamlMetastring="{4-10}"
	jsonMetastring="{5-7}"
	config={{
		on_tcp_connect: [
			{
				actions: [
					{
						type: "deny",
					},
				],
			},
		],
	}}
/>

#### Example Request

```shell
$ curl 12e461b71f86488a.ngrok.app:443
curl: (52) Empty reply from server
```

In this example, we attempt to connect to `12e461b71f86488a.ngrok.app:443` using
the `curl` command and ngrok immediately closes the connection with an empty
reply.
