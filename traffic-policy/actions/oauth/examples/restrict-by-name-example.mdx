import ConfigExample from "/src/components/ConfigExample.tsx";

<ConfigExample
	snippetText="Only allow access to users named 'My Name'. Add this after your OAuth Action."
	showLineNumbers={true}
	config={{
		on_http_request: [
			{
				expressions: ["!(actions.ngrok.oauth.identity.name in ['My Name'])"],
				actions: [
					{
						type: "deny",
					},
				],
			},
		],
	}}
/>
