## Overview

The **Forward Internal** Traffic Policy action forwards traffic to an endpoint within the same ngrok account. This is useful for routing traffic to private services through a single public endpoint.
