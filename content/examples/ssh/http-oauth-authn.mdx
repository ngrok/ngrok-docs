```bash
ssh -R 443:localhost:80 v2@connect.ngrok-agent.com http --traffic-policy-file=oauth-policy.yml
```

Create the `oauth-policy.yml` file:

```yaml
on_http_request:
  - actions:
      - type: oauth
        config:
          provider: google
```
