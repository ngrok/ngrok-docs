import ConfigExample from "../@components/ConfigExample";

export const BasedOnHeaders = () => (
	<ConfigExample
		config={{
			on_http_request: {
				expressions: ["('proxy.anonymous.tor' in conn.client_ip.categories)"],
				actions: {
					type: "deny",
					code: 403,
				},
			},
		}}
	/>
);
