```bash
ngrok tls 80 \
  --url tls://app.example.com \
  --traffic-policy-file traffic-policy.yml
```

##### `traffic-policy.yml`

```yaml
on_tcp_connect:
  - actions:
      - type: terminate-tls
        config:
          mutual_tls_certificate_authorities: |
            -----BEGIN CERTIFICATE-----
            ... snip ...
            -----END CERTIFICATE-----
```
