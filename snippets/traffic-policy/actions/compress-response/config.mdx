import { Config, ConfigEnum, ConfigEnumOption, ConfigField } from "/snippets/ConfigTable.jsx";

## Configuration Reference

The [Traffic Policy](/traffic-policy/) configuration reference for this action.

### Supported Phases

`on_http_response`

### Type

`compress-response`

### Configuration Fields

<Config>
  <ConfigField title="algorithms" type="array of strings">
    <p>A list of allowed compression algorithms to be considered during encoding type negotiation. Each element must be unique.</p>
    <p>Defaults to `gzip`, `deflate`, `br`, `compress`.</p>
    <ConfigEnum label="Supported algorithms">
      <ConfigEnumOption>`br`</ConfigEnumOption>
      <ConfigEnumOption>`compress`</ConfigEnumOption>
      <ConfigEnumOption>`deflate`</ConfigEnumOption>
      <ConfigEnumOption>`gzip`</ConfigEnumOption>
    </ConfigEnum>
  </ConfigField>
</Config>
