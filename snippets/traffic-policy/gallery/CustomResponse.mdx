  <CodeGroup>
    ```json policy.json
    {
      "on_http_request": [
        {
          "expressions": ["!('authorization' in req.headers)"],
          "actions": [
            {
              "type": "custom-response",
              "config": {
                "status_code": 401,
                "body": "Unauthorized"
              }
            }
          ]
        }
      ]
    }
    ```

    ```yaml policy.yml
    on_http_request:
      - expressions:
          - "!('authorization' in req.headers)"
        actions:
          - type: custom-response
            config:
              status_code: 401
              body: Unauthorized
    ```

  </CodeGroup>
