  <CodeGroup>
    ```json policy.json
    {
      "on_http_request": [
        {
          "expressions": [],
          "name": "Add headers to requests",
          "actions": [
            {
              "type": "add-headers",
              "config": {
                "headers": {
                  "is-ngrok": "1",
                  "country": "${.ngrok.geo.country_code}"
                }
              }
            }
          ]
        }
      ],
      "on_http_response": []
    }
    ```

    ```yaml policy.yml
    on_http_request:
      - expressions: []
        name: Add headers to requests
        actions:
          - type: add-headers
            config:
              headers:
                is-ngrok: '1'
                country: ${.ngrok.geo.country_code}
    on_http_response: []
    ```

  </CodeGroup>
