# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local
.env

# ignore debug files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ignore yarn lock since we use pnpm
yarn.lock*

# pnpm uses npm for publishing a new version with
# dependencies bundled but the npm lockfile is not needed
# because pnpm use pnpm for installation
package-lock.json

*.zip
!static/other/ngrok-logos-2023.01.22.zip
# ide specific
.idea
.vscode
/.direnv
