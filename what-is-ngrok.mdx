---
title: What is ngrok?
description: ngrok is developer infrastructure that routes and secures traffic to your apps, APIs, and AI models.
---

ngrok is developer infrastructure that routes and secures traffic to your apps, APIs, and AI models. 
We give you ways to manage connectivity between all of the tools and platforms you use to put your services online, no matter where they're deployed or what network they're on. 

ngrok's core products are Gateway, AI Gateway, and Share localhost.
Key tools and services like the ngrok agent, Traffic Policy, and Traffic Inspector are shared across products.

## Gateway

ngrok Gateway is an all-in-one cloud networking platform that secures, transforms, and routes traffic to all your services, no matter where they run.
As opposed to cloud provider gateways, which are locked to services running in their networks, ngrok works the same in AWS, Azure, GCP, on-prem, and your customer's networks.
That means you get one cloud-agnostic way to add public/private connectivity to any service, transform traffic, enforce authentication, or observe traffic.
If your services span environments, ngrok is one gateway in front of all of them instead of one per cloud.

## AI Gateway

With the ngrok AI Gateway, you can route, secure, and manage traffic to any LLM—cloud or local—on one unified platform.
Use OpenAI, Anthropic, and more without signing up for each provider—ngrok manages the provider keys for you.
Key features include automatic failover, load balancing, and observability.

## Share localhost

Securely expose a web server, or any other service you're running on `localhost`, to the internet, even from behind a NAT or firewall, with secure tunnels.
ngrok assigns a public endpoint that forwards incoming requests to your local server over an encrypted connection. 
Skip port forwarding, DNS changes, and firewall rules entirely.
Use it to demo work without deploying, test webhooks against your local server, hit a dev build from a mobile device, or let ChatGPT, Claude, and other AI clients talk to an MCP server running on your laptop.

## What can you do with ngrok?

TK
