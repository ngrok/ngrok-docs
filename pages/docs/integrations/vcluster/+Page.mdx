---
name: vcluster
title: vcluster Integration Hub
sidebar_label: vcluster
description: |
  vcluster is an open source project for creating virtual clusters that run inside regular namespaces, which provides strong isolation and easy access for multiple tenants with low cost and overhead. ngrok and vcluster compliment one another to simplify how developers add and control traffic ingress to applications running in a virtual cluster.
excerpt: |
  Quickly add ingress traffic to applications running in virtual clusters.
---

import IntegrationPageList from "@components/IntegrationPageList";

<p>{frontmatter.description}</p>

<IntegrationPageList name={frontmatter.name} />
