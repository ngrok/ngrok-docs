Navigate to the [**Domains** section](https://dashboard.ngrok.com/domains) of the ngrok dashboard to use your free dev domain, click **New +** to reserve a custom domain like <code className="border-card rounded-md border bg-gray-500/5 px-1 py-0.5 font-mono text-[0.8em]">{props.domain ? props.domain : 'https://your-service.ngrok.app'}</code>, or use a [custom domain](/universal-gateway/custom-domains/) you already own.

We'll refer to this domain as `$NGROK_DOMAIN` from here on out.
