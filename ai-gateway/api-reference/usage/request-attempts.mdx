---
openapi: "/openapi/ai-gateway.yaml GET /usage/requests/{requestId}"
description: Get every upstream attempt the AI Gateway made for one client request, ordered by attempt number.
---

{/* Unpublished: gated on ngrok PR #47372. Add to docs.json navigation when that merges. */}
