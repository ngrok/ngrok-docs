---
title: Replay Requests
sidebarTitle: Replay Requests
description: Replay captured HTTP requests to speed up debugging and development with ngrok's traffic inspector.
---

ngrok's traffic inspector lets you replay any captured HTTP request with a single click. This is especially powerful when debugging webhooks, where you'd otherwise need to wait for the provider to resend the event.

## How replay works

1. Start your tunnel with `ngrok http <port>`
2. Open the traffic inspector at `http://127.0.0.1:4040`
3. Trigger a request (or wait for a webhook delivery)
4. Click **Replay** on any captured request to resend it to your local server

The replayed request is sent with the same method, headers, and body as the original, giving you a consistent way to test your handler without external dependencies.

## Use cases

- **Webhook development:** Replay webhook deliveries instead of waiting for the provider to retry
- **Bug reproduction:** Capture a request that triggers a bug, fix your code, and replay to verify the fix
- **Load testing:** Replay requests multiple times to observe how your application handles repeated traffic

## Modifying replayed requests

You can edit a request before replaying it—change headers, query parameters, or the request body to test different scenarios without needing external tools like cURL or Postman.

## Next steps

- [Traffic inspection](/guides/share-localhost/inspection) — Learn more about inspecting traffic in real time
- [Webhooks](/guides/share-localhost/webhooks) — Use replay to accelerate webhook development
- [Quickstart](/guides/share-localhost/quickstart) — Get started with ngrok
