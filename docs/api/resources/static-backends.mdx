
import StaticBackendsCreateRequest from './examples/_static_backends_create_request.md';
import StaticBackendsCreateResponse from './examples/_static_backends_create_response.md';
import StaticBackendsDeleteRequest from './examples/_static_backends_delete_request.md';
import StaticBackendsDeleteResponse from './examples/_static_backends_delete_response.md';
import StaticBackendsGetRequest from './examples/_static_backends_get_request.md';
import StaticBackendsGetResponse from './examples/_static_backends_get_response.md';
import StaticBackendsListRequest from './examples/_static_backends_list_request.md';
import StaticBackendsListResponse from './examples/_static_backends_list_response.md';
import StaticBackendsUpdateRequest from './examples/_static_backends_update_request.md';
import StaticBackendsUpdateResponse from './examples/_static_backends_update_response.md';

# Static Backends

## Create Static Backend

Create a new static backend

### Request

POST /backends/static

<StaticBackendsCreateRequest />

#### Parameters

| Name | Type | Description |
|---|---|---|
| `description` | string | human-readable description of this backend. Optional |
| `metadata` | string | arbitrary user-defined machine-readable data of this backend. Optional |
| `address` | string | the address to forward to |
| `tls` | [StaticBackendTLS](#staticbackendtls-parameters) | tls configuration to use |

#### StaticBackendTLS parameters

| Name | Type | Description |
|---|---|---|
| `enabled` | boolean | if TLS is checked |
### Response

Returns a 201 response on success

<StaticBackendsCreateResponse />

#### Fields

| Name | Type | Description |
|---|---|---|
| `id` | string | unique identifier for this static backend |
| `uri` | string | URI of the StaticBackend API resource |
| `created_at` | string | timestamp when the backend was created, RFC 3339 format |
| `description` | string | human-readable description of this backend. Optional |
| `metadata` | string | arbitrary user-defined machine-readable data of this backend. Optional |
| `address` | string | the address to forward to |
| `tls` | [StaticBackendTLS](#staticbackendtls-fields) | tls configuration to use |

#### StaticBackendTLS fields

| Name | Type | Description |
|---|---|---|
| `enabled` | boolean | if TLS is checked |

## Delete Static Backend

Delete a static backend by ID.

### Request

DELETE /backends/static/\{id\}

<StaticBackendsDeleteRequest />

### Response

Returns a 204 response with no body on success

## Get Static Backend

Get detailed information about a static backend by ID

### Request

GET /backends/static/\{id\}

<StaticBackendsGetRequest />

### Response

Returns a 200 response on success

<StaticBackendsGetResponse />

#### Fields

| Name | Type | Description |
|---|---|---|
| `id` | string | unique identifier for this static backend |
| `uri` | string | URI of the StaticBackend API resource |
| `created_at` | string | timestamp when the backend was created, RFC 3339 format |
| `description` | string | human-readable description of this backend. Optional |
| `metadata` | string | arbitrary user-defined machine-readable data of this backend. Optional |
| `address` | string | the address to forward to |
| `tls` | [StaticBackendTLS](#staticbackendtls-fields) | tls configuration to use |

#### StaticBackendTLS fields

| Name | Type | Description |
|---|---|---|
| `enabled` | boolean | if TLS is checked |

## List Static Backends

List all static backends on this account

### Request

GET /backends/static

<StaticBackendsListRequest />

### Response

Returns a 200 response on success

<StaticBackendsListResponse />

#### Fields

| Name | Type | Description |
|---|---|---|
| `backends` | [StaticBackend](#staticbackend-fields) | the list of all static backends on this account |
| `uri` | string | URI of the static backends list API resource |
| `next_page_uri` | string | URI of the next page, or null if there is no next page |

#### StaticBackend fields

| Name | Type | Description |
|---|---|---|
| `id` | string | unique identifier for this static backend |
| `uri` | string | URI of the StaticBackend API resource |
| `created_at` | string | timestamp when the backend was created, RFC 3339 format |
| `description` | string | human-readable description of this backend. Optional |
| `metadata` | string | arbitrary user-defined machine-readable data of this backend. Optional |
| `address` | string | the address to forward to |
| `tls` | [StaticBackendTLS](#staticbackendtls-fields) | tls configuration to use |
#### StaticBackendTLS fields

| Name | Type | Description |
|---|---|---|
| `enabled` | boolean | if TLS is checked |

## Update Static Backend

Update static backend by ID

### Request

PATCH /backends/static/\{id\}

<StaticBackendsUpdateRequest />

#### Parameters

| Name | Type | Description |
|---|---|---|
| `id` | string |  |
| `description` | string | human-readable description of this backend. Optional |
| `metadata` | string | arbitrary user-defined machine-readable data of this backend. Optional |
| `address` | string | the address to forward to |
| `tls` | [StaticBackendTLS](#staticbackendtls-parameters) | tls configuration to use |

#### StaticBackendTLS parameters

| Name | Type | Description |
|---|---|---|
| `enabled` | boolean | if TLS is checked |
### Response

Returns a 200 response on success

<StaticBackendsUpdateResponse />

#### Fields

| Name | Type | Description |
|---|---|---|
| `id` | string | unique identifier for this static backend |
| `uri` | string | URI of the StaticBackend API resource |
| `created_at` | string | timestamp when the backend was created, RFC 3339 format |
| `description` | string | human-readable description of this backend. Optional |
| `metadata` | string | arbitrary user-defined machine-readable data of this backend. Optional |
| `address` | string | the address to forward to |
| `tls` | [StaticBackendTLS](#staticbackendtls-fields) | tls configuration to use |

#### StaticBackendTLS fields

| Name | Type | Description |
|---|---|---|
| `enabled` | boolean | if TLS is checked |
