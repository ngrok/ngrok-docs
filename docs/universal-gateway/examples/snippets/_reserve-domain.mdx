Navigate to the [**Domains** section](https://dashboard.ngrok.com/domains) of the ngrok dashboard and click **New +** to reserve a free static domain like <code className="border-card rounded-md border bg-gray-500/5 px-1 py-0.5 font-mono text-[0.8em]">{props.domain ? props.domain : 'https://your-service.ngrok.app'}</code> or a [custom domain](/docs/universal-gateway/custom-domains/) you already own.

We'll refer to this domain as `$NGROK_DOMAIN` from here on out.
