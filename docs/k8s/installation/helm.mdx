---
title: Helm
pagination_next: k8s/installation/install
---

import RemoteYamlBlock from "@site/src/components/RemoteYamlBlock";

# Helm Configuration

The following values can be used when installing/upgrading the ngrok operator using `Helm`

The following YAML block contains the default `values.yaml` used when installing the ngrok Kubernetes Operator.
You can reference these values and change them individually with `--set` flags while installing/upgrading the ngrok Kubernetes operator.

<RemoteYamlBlock
	url="https://raw.githubusercontent.com/ngrok/ngrok-operator/main/helm/ngrok-operator/values.yaml"
	title="values.yaml"
/>
