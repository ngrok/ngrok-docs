```yaml
version: 3
endpoints:
  - name: example
    url: tls://example.ngrok.app
    upstream:
      url: tls://localhost:443
    traffic_policy:
      on_tcp_connect:
        - actions:
            - type: terminate-tls
```
