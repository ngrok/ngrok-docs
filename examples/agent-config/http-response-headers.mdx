```yaml
tunnels:
  example:
    proto: http
    addr: 80
    response_header:
      add:
        - "content-security-policy: self"
        - "dial-duration: ${.backend.dial_duration}"
      remove:
        - "internal-trace-id"
```
