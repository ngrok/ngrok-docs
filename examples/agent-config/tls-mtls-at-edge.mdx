```yaml
version: 3
endpoints:
  - name: example
    url: tls://app.example.com"
    upstream:
      url: 80
    traffic_policy:
      on_tcp_connect:
        - actions:
            - type: terminate-tls
              config:
                mutual_tls_certificate_authorities: [ca_a1b2c3d4e5f6]
```
