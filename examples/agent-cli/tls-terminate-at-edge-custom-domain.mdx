```bash
ngrok tls 80 \
  --url tls://your-name.ngrok.app \
  --traffic-policy-file traffic-policy.yml
```

##### `traffic-policy.yml`

```yaml
on_tcp_connect:
  - actions:
      - type: terminate-tls
```
