```jsx
const ngrok = require("@ngrok/ngrok");

(async function () {
	const listener = await ngrok.connect({
		addr: 8080,
		authtoken_from_env: true,
		proto: "tls",
		proxy_proto: "2",
	});

	console.log(`Ingress established at: ${listener.url()}`);
})();
```

NodeJS SDK Docs:

- [https://ngrok.github.io/ngrok-nodejs/interfaces/Config.html#proxy_proto](https://ngrok.github.io/ngrok-nodejs/interfaces/Config.html#proxy_proto)
- [https://ngrok.github.io/ngrok-nodejs/classes/TlsListenerBuilder.html#proxyProto](https://ngrok.github.io/ngrok-nodejs/classes/TlsListenerBuilder.html#proxyProto)
