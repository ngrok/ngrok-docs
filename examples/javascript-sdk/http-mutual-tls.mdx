```jsx
const ngrok = require("@ngrok/ngrok");
const fs = require("fs");

(async function () {
	const listener = await ngrok.forward({
		addr: 8080,
		authtoken_from_env: true,
		mutual_tls_cas: [fs.readFileSync("/path/to/cas.pem", "utf8")],
	});

	console.log(`Ingress established at: ${listener.url()}`);
})();
```

Javascript SDK Docs:

- [https://ngrok.github.io/ngrok-javascript/interfaces/Config.html#mutual_tls_cas](https://ngrok.github.io/ngrok-javascript/interfaces/Config.html#mutual_tls_cas)
- [https://ngrok.github.io/ngrok-javascript/classes/HttpListenerBuilder.html#mutualTlsca](https://ngrok.github.io/ngrok-javascript/classes/HttpListenerBuilder.html#mutualTlsca)
