import ConfigExample from "/src/components/ConfigExample.tsx";

export const type = "remove-headers";
export const config = {
	headers: ["is-ngrok", "internal-trace-id"],
};

export const ExampleHTTP = () => (
	<ConfigExample config={{ actions: [{ type, config }] }} />
);
