```go
import (
	"context"
	"net"

	"golang.ngrok.com/ngrok"
	"golang.ngrok.com/ngrok/config"
)

func ngrokListener(ctx context.Context) (net.Listener, error) {
	return ngrok.Listen(ctx,
		config.HTTPEndpoint(
			config.WithOAuth("google",
				config.WithAllowOAuthEmail("kate.libby@gmail.com"),
				config.WithAllowOAuthDomain("acme.org"),
			),
		),
		ngrok.WithAuthtokenFromEnv(),
	)
}
```

Go Package Docs:

- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithAllowOAuthDomain](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithAllowOAuthDomain)
- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithAllowOAuthEmail](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithAllowOAuthEmail)
