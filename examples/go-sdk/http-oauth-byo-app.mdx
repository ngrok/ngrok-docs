```go
import (
	"context"
	"net"

	"golang.ngrok.com/ngrok"
	"golang.ngrok.com/ngrok/config"
)

func ngrokListener(ctx context.Context) (net.Listener, error) {
	return ngrok.Listen(ctx,
		config.HTTPEndpoint(
			config.WithOAuth("microsoft"),
			config.WithOAuthClientID("{client id}"),
			config.WithOAuthClientSecret("{client secret}"),
		),
		ngrok.WithAuthtokenFromEnv(),
	)
}
```

Go Package Docs:

- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithOAuthClientID](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithOAuthClientID)
- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithOAuthClientSecret](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithOAuthClientSecret)
