```go
import (
        "context"
        "net"

        "golang.ngrok.com/ngrok"
        "golang.ngrok.com/ngrok/config"
)

func ngrokListener(ctx context.Context) (net.Listener, error) {
        return ngrok.Listen(ctx,
                config.TLSEndpoint(
                        config.WithDomain("your-name.ngrok.app"),
                        config.WithTLSTermination(
                                config.WithTLSTerminationAt(config.TLSAtEdge),
                        ),
                ),
                ngrok.WithAuthtokenFromEnv(),
        )
}
```

Go Package Docs:

- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithDomain](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithDomain)
- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithTLSTerminationAt](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithTLSTerminationAt)
