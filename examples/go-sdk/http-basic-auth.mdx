```go
import (
	"context"
	"net"

	"golang.ngrok.com/ngrok"
	"golang.ngrok.com/ngrok/config"
)

func ngrokListener(ctx context.Context) (net.Listener, error) {
	return ngrok.Listen(ctx,
		config.HTTPEndpoint(
			config.WithBasicAuth("username1", "password1"),
			config.WithBasicAuth("username2", "password2"),
		),
		ngrok.WithAuthtokenFromEnv(),
	)
}
```

Go Package Docs:

- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithBasicAuth](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithBasicAuth)
