```go
import (
	"context"
	"net"
	"os"

	"golang.ngrok.com/ngrok/v2"
)

func ngrokListener(ctx context.Context) (net.Listener, error) {
	agent, _ := ngrok.NewAgent(
		ngrok.WithAuthtoken(os.Getenv("NGROK_AUTHTOKEN")),
		ngrok.WithAgentConnectURL("tunnel.us.ingress.example.com:443"),
	)
	return agent.Listen(ctx)
}
```

Go Package Docs:

- [https://pkg.go.dev/golang.ngrok.com/ngrok/v2#WithAgentConnectURL](https://pkg.go.dev/golang.ngrok.com/ngrok/v2#WithAgentConnectURL)
