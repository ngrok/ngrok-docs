```go
import (
	"context"
	"net"

	"golang.ngrok.com/ngrok"
	"golang.ngrok.com/ngrok/config"
)

func ngrokListener(ctx context.Context) (net.Listener, error) {
        return ngrok.Listen(ctx,
                config.HTTPEndpoint(
                        config.WithAllowUserAgent(`(foo)/(\d)+.(\d)+`),
                        config.WithDenyUserAgent(`(bar)/(\d)+.(\d)+`),
                ),
                ngrok.WithAuthtokenFromEnv(),
        )
}
```

Go Package Docs:

- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithAllowUserAgent](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithAllowUserAgent)
- [https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithDenyUserAgent](https://pkg.go.dev/golang.ngrok.com/ngrok/config#WithDenyUserAgent)
