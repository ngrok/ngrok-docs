---
title: Agent API Keys
sidebarTitle: API keys
description: Add an API key to the agent so you can query and manage domains, endpoints, and other account resources from the command line.
---

The ngrok agent contains a [complete CLI for the ngrok API](/gateway/agent/cli-api/).

To use it, [create an API Key on your ngrok dashboard](https://dashboard.ngrok.com/api-keys) and then run:

```bash
ngrok config add-api-key <API KEY>
```

After you've installed the API Key, try querying your list of domains or online endpoints:

```bash
ngrok api endpoints list
ngrok api reserved-domains list
```
